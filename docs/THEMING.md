# Theming

The theme is centralized in `src/design-system/theme.ts`.

Change the product identity by editing semantic tokens, not individual screens. Future themes such as Space, Jungle, Ocean, or Vehicles can be added by swapping the provider value.
