# Testing

Run:

```sh
npm test
npx tsc --noEmit
npm run lint
```

Add unit tests for game rules, scoring, rewards, progress, localization, language persistence, and ad frequency. Add component tests for reusable controls before replacing current service abstractions with native integrations.
