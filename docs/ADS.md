# Ads

Ads are disabled by default for the child-safe foundation.

Future monetization should remain policy-aware: no ads that interrupt young children, no pressure loops, no sensitive tracking, and all SDK calls behind `AdService`.
