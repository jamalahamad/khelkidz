# Rewards

`RewardManager` calculates stars, coins, XP, badges, and level completion rewards from a `LevelResult`.

`ProgressManager` applies rewards to namespaced game progress. `KKRewardPopup` renders the shared celebration flow for all games.
